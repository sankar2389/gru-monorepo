module.exports = {
  query: `
        buys (offset: Int, limit: Int): [Buy!]!
    `,
  resolver: {
    Query: {
      buysByOffset: {
            resolver: (root, { offset, limit }, ctx, info) => {
                return buys.slice(offset, offset + limit)
        }
      }
    }
  }
}